#include <bits/stdc++.h> 
void sort012(int *arr, int n)
{
   int a=0;
   for(int i=0;i<n;i++){
       if(arr[i]==0){
           swap(arr[i],arr[a]);
           a++;
       }
   }
   for(int i=a;i<n;i++){
       if(arr[i]==1){
           swap(arr[i],arr[a]);
           a++;
       }
   }
}
